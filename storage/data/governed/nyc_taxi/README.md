# NYC Taxi benchmark fixture

The benchmark expects `yellow_tripdata_2024-01.parquet` in this directory. The
dataset is intentionally not stored in Git. Provision it identically on both test
runs and record its SHA-256 digest in the experiment manifest.
